# .npmrc - ignore peer dependency conflicts (use legacy peer deps)
legacy-peer-deps=true

# Optional: set the registry explicitly (usually default)
registry=https://registry.npmjs.org/

# Optional: reduce log verbosity during install
loglevel=error
